---

**1. Agent vs Workflow的区别是什么？**  
**Workflow**：预定义的、固定的执行流程，由一系列步骤组成，每一步做什么、何时做都是事先确定好的（如 DAG、状态机）。确定性强，输出可预期，适合简单、稳定、重复的任务，用代码逻辑编排（如 Airflow）实现，灵活性低，需人工调整流程。  
**Agent**：智能体，能根据当前环境和目标动态决策下一步行动，可使用工具、调用 LLM、观察结果，循环直至目标完成。动态性强，输出不确定，适合复杂、多变、需推理的任务，用 LLM + 规划 + 工具调用（如 ReAct）实现，灵活性高，能自主适应环境变化，但可控性相对较低。

---

**2. 在什么场景下选 Workflow？什么场景下选 Agent？**  
- **选 Workflow**：订单处理、定时报表、数据 ETL、审批流程、任何确定性强、步骤固定的任务。  
- **选 Agent**：智能客服、代码调试助手、研究型助手、个人助理、需要动态决策和工具调用的复杂任务。

---

**3. 企业级 Agent 架构怎么设计？**  
企业级 Agent 架构通常采用微服务拆分，包含以下核心组件：  
- **BFF（Backend for Frontend）**：与客户端交互，处理身份认证、请求路由、限流熔断。  
- **Worker（协调层）**：无状态服务，执行 Agent 核心逻辑（规划、记忆管理、工具调度），可水平扩展。  
- **Tool Server（工具层）**：封装各类工具（搜索、数据库、API），以服务形式暴露，实现认证、限流、监控。  
- **数据层**：存储对话历史、知识库、用户数据等（关系库 + 向量库）。

---

**4. 企业级 Agent 架构中如何进行多组件拆分？**  
多组件拆分通常包括：  
- **BFF**：负责前端适配、聚合。  
- **Worker**：核心决策和执行。  
- **Tool Server**：工具服务。  
- **数据层**：持久化。  
组件之间通过 API 网关通信，使用消息队列（如 Kafka）解耦耗时任务。

---

**5. 企业级 Agent 架构中安全与隔离怎么做？**  
- **网络隔离**：组件部署在不同 VPC/子网，通过 API 网关通信。  
- **身份与权限**：服务间 JWT/OAuth 认证；RBAC 控制工具调用权限；多租户数据行级隔离。  
- **审计与日志**：记录所有请求、决策、工具调用（时间、用户、输入输出），敏感信息脱敏。  
- **限流与熔断**：防止单个用户滥用或工具过载。  
- **工具沙箱**：在容器或虚拟机中执行不可信代码，限制资源使用。

---

**6. 企业级 Agent 架构中权限、审计、日志如何实现？**  
- **权限**：RBAC 模型，用户/角色绑定权限，工具调用时检查权限。  
- **审计**：将所有关键操作（如工具调用、数据访问）记录到审计系统，支持追溯。  
- **日志**：结构化日志，包含请求 ID、时间、用户、输入输出等，便于监控和排障。

---

**7. MCP（Model Context Protocol）是什么？**  
Anthropic 提出的开放协议，标准化 LLM 与外部工具/数据源的交互，类似 AI 界的“USB-C”。通过 MCP，模型可以统一发现、调用工具和读取资源，无需为每个工具编写适配器。

---

**8. MCP 有哪些分类？**  
- **Resources**：静态数据源（文件、数据库、API 返回数据），模型可读取。  
- **Tools**：可执行函数，模型可调用操作。

---

**9. MCP Server 如何开发？**  
使用官方 SDK（Python/TypeScript），实现资源列表、工具列表及对应处理函数，通过 stdio 或 HTTP 通信。Server 需实现标准接口，如 `list_tools`、`call_tool`、`read_resource` 等。开发流程包括：1）安装 MCP SDK；2）创建 Server 类并实现生命周期方法；3）注册资源和工具处理器；4）配置传输层（stdio/SSE）；5）启动服务并测试。

---

**10. MCP 的 Transport 层有哪些？**  
- **stdio**：本地进程通信。  
- **SSE（Server-Sent Events）**：服务器向客户端单向推送。  
- **Streamable HTTP**：支持请求-响应和流式传输。  
- （未来可能支持 WebSocket）

---

**11. 为什么 MCP 在 Agent 生态里重要？**  
- 统一工具接入标准，避免重复开发适配器。  
- 促进 Agent 生态互操作性，工具一次实现，随处可用。  
- 简化 Agent 开发，像调用本地函数一样调用远程工具。  
- 通过协议层实现权限控制和安全策略。

---

**12. 如何理解与评价 Claude Code / Codex / Manus 这类 Agent 产品？**  
这些产品代表从“对话模型”向“自主执行 Agent”的演进。它们能分解复杂任务、调用工具、自我反思，最终完成编码、研究等任务。相比传统 Chat Agent，它们具备主动规划、持久化任务状态、错误恢复等能力，是迈向通用智能体的重要一步。

---

**13. 这类 Agent 产品的架构逻辑是什么？**  
- **规划模块**：将任务拆解为步骤，支持动态调整。  
- **工具调用**：集成终端、文件系统、浏览器、API 等。  
- **记忆模块**：保存对话历史和任务状态。  
- **执行监控**：跟踪任务进度，处理异常情况。  
- **自我反思**：根据执行结果调整计划，形成反馈闭环。

---

**14. 这类 Agent 产品与传统 Chat Agent 的差别是什么？**  
- 传统 Agent 被动响应，新 Agent 主动规划。  
- 传统 Agent 仅调用预设工具，新 Agent 可动态选择甚至编写代码。  
- 新 Agent 具备持久化任务状态和错误恢复能力。

---

**15. 这类 Agent 产品的工程能力如何实现？**  
- 强大的基础模型（低延迟、高并发）。  
- 安全沙箱（容器隔离）执行工具。  
- 上下文管理（处理长对话）。  
- 错误重试与反馈回路。

---

**16. Claude 的 Deep Research 方法论要配备哪些工具？**  
- 搜索引擎（如 Bing API）  
- 网页抓取工具（浏览器渲染、HTML 解析）  
- 文档阅读器（PDF、Word）  
- 知识库查询  
- 代码执行环境（数据分析）  
- 数据可视化工具（生成图表辅助分析）

---

**17. Deep Research 的基础设施如何搭建？**  
- **任务调度**：消息队列（Kafka）+ 工作节点管理长期任务。  
- **工具服务**：微服务封装工具，限流、缓存。  
- **安全隔离**：Firecracker/Docker 沙箱运行抓取和代码。  
- **存储**：向量数据库存研究结果，关系数据库存任务状态。

---

**18. Deep Research 的安全和基建挑战是什么？**  
- **工具滥用**：权限控制 + 内容过滤。  
- **数据隐私**：加密存储与传输。  
- **成本控制**：限流、预算监控。  
- **可靠性**：任务持久化、断点续传。

---

**19. 多轮对话中的短期记忆怎么实现？**  
短期记忆对应当前对话轮次的上下文，通常通过将对话历史拼接到 prompt 中实现。受限于窗口长度，可采用滑动窗口或摘要压缩来保留最近的重要信息。

---

**20. 多轮对话中的长期记忆怎么实现？**  
长期记忆需持久化存储，常用方法：  
- 提取重要信息（事实、偏好）存入键值数据库。  
- 向量数据库存储对话片段，通过语义检索召回。  
- 定期生成对话摘要并存储。

---

**21. 多轮对话中的任务记忆是什么？如何实现？**  
任务记忆指当前任务的中间状态（已完成步骤、剩余步骤、结果）。实现方式：在 prompt 中保持状态描述，或用外部存储（Redis）保存状态，每次调用时更新。

---

**22. 上下文机制的完整实现流程是什么？**  
- **写入（Memory Gating）**：通过重要性评分、类型过滤、去重决定哪些信息进入记忆。  
- **读取（Retrieval）**：结合时效性（最近消息优先）和相关性（向量检索）获取候选记忆。  
- **重排序（Rerank）**：对候选记忆用模型评分，选出最相关的若干条。  
- **状态管理**：使用结构化事实存储，定期一致性检查，Prompt 中明确"基于已知事实"。中明确“基于已知事实”。

---

**23. 上下文机制的写入策略（memory gating）有哪些？**  
- 重要性评分（LLM 判断）。  
- 基于类型过滤（指令、关键事实保留，闲聊丢弃）。  
- 去重（避免重复存储）。

---

**24. 上下文机制的读取策略有哪些？**  
- **时效性（Recency）**：最近的消息优先。  
- **相关性（Relevance）**：向量检索找到与当前查询语义相近的记忆。

---

**25. 上下文机制中如何做重排序（rerank）？**  
结合时效性和相关性，或用模型对候选记忆评分，选出最合适的若干条，例如使用交叉编码器计算相关性分数后排序。

---

**26. 如何保证上下文状态稳定？**  
- 使用结构化记忆（事实三元组）。  
- 一致性检查，修正矛盾信息。  
- Prompt 中明确“基于已知事实，忽略过时信息”。

---

**27. 超 token 怎么处理？**  
常用方法：  
- **Rolling Summary**：将早期内容摘要，替换原始对话。  
- **State Extraction**：提取关键状态（意图、已完成任务）结构化表示。  
- **RAG/Retrieval**：将历史存入向量库，每次查询检索相关片段。  
- **分层压缩**：远距离对话粗粒度摘要，近距离保留细节。

---

**28. 如何设计一个高效的 Agent 上下文维护方案？**  
- 短期：滑动窗口 + 摘要。  
- 长期：RAG 检索。  
- 关键状态：结构化存储。  
- 缓存减少检索延迟。

---

**29. 你认为维护上下文的关键问题是什么？**  
- 信息丢失与冗余的平衡。  
- 检索准确性。  
- 记忆冲突。  
- 延迟开销。

---

**30. 如何处理上下文丢失的情况？**  
- 提示用户重新提供。  
- 从长期记忆尝试恢复。  
- 重新推理或使用通用知识。

---

**31. 目前主流的 AI 模型支持的上下文长度大概是多少？**  
- GPT-4 Turbo：128k  
- Claude 3：200k  
- Gemini 1.5：1M  
- DeepSeek：1M

---

**32. 如果需要处理更长的上下文，有什么优化方案？**  
- 流式处理 + 分层记忆。  
- 模型本身支持更长窗口（如 1M token）。  
- 使用 RAG 外部检索替代全量输入。

---

**33. 一个完整的 Agent 智能体架构一般包括哪些部分？**  
- 用户交互界面  
- 规划模块  
- 记忆模块（短期+长期）  
- 工具调用模块  
- 执行模块  
- 反馈/反思模块  
- 安全与监控模块：权限控制、审计日志、异常检测

---

**34. 如何设计 Agent 的模块化结构来提高扩展性和可维护性？**  
- 微服务架构，独立部署。  
- 定义清晰接口（如 MCP）。  
- 插件机制添加新工具。  
- 配置化动态调整行为。

---

**35. Agent 和 LLM 有什么区别？**  
LLM 是语言模型，Agent 是包含 LLM 的完整系统，集成了规划、记忆、工具等能力。Agent 利用 LLM 进行理解和生成，但还具备执行、调用外部工具等自主行为。

---

**36. Agent 中是否包含一些 LLM 的功能？它们如何协作？**  
Agent 包含 LLM 作为核心推理引擎。协作方式：Agent 框架调用 LLM 进行理解、生成、推理，解析输出，执行工具，并将结果反馈给 LLM 继续决策。

---

**37. 向量检索和关键词检索的区别是什么？**  
- **向量检索**：基于语义相似度，理解上下文，能处理同义词、模糊匹配，但需要训练好的 embedding 模型，计算成本较高。  
- **关键词检索**：基于字面匹配，精确但无语义，适合代码、专名等精确查询，速度快但召回率受限。  
- **混合检索**：实际应用中常结合两者，先用关键词粗筛，再用向量精排，平衡效率与效果。

---

**38. 如何选择检索方式，取决于什么因素？**  
取决于任务类型：需要语义理解时（如开放域问答）用向量检索；需要精确匹配（如代码、ID）用关键词检索。也可混合检索（先关键词筛选，再向量重排）。

---

**39. 你了解的 tool 调用机制是怎样的？**  
LLM 输出结构化指令（如 JSON 含工具名、参数），Agent 解析后调用对应函数，返回结果给 LLM，LLM 根据结果继续生成最终答案或下一步调用。

---

**40. 如何确保调用过程中的错误处理与异常捕获？**  
- try-catch 捕获异常，错误信息返回 LLM 决定重试或调整。  
- 超时、重试机制。  
- 参数预验证。  
- 错误日志记录。

---

**41. 如何定位 MySQL 的慢查询问题？**  
- 开启慢查询日志，设置 `long_query_time` 和 `log_queries_not_using_indexes`。  
- 用 `EXPLAIN` 分析执行计划，关注 type、key、rows、Extra 字段。  
- 使用工具：`pt-query-digest` 分析日志模式、`mysqldumpslow` 排序慢查询。  
- 监控维度：CPU、IO、锁等待、连接数、缓存命中率。  
- 业务层面：分析高峰时段、热点表、批量操作影响。

---

**42. 在优化 MySQL 慢查询过程中，有没有特别常用的工具或者策略？**  
**工具**：`EXPLAIN`、`pt-query-digest`、`Percona Toolkit`、`MySQL Workbench`。  
**策略**：添加索引、优化 SQL、分库分表、读写分离、缓存。

---

**43. MySQL 中的事务隔离级别有哪些？**  
- 读未提交  
- 读已提交  
- 可重复读（MySQL 默认）  
- 串行化

---

**44. 如何选择合适的事务隔离级别来保证数据一致性？**  
根据业务对幻读/脏读的容忍度选择。可重复读适用于多数场景（如金融交易），串行化用于极端一致性要求，但并发低。

---

**45. 什么是索引覆盖？它如何优化查询性能？**  
索引覆盖指查询所需列都在索引中，无需回表，减少 IO，提升查询速度。例如 `SELECT id, name FROM user WHERE age > 18`，如果索引 (age, id, name) 覆盖了所有查询列，则直接使用索引返回结果。

---

**46. 在什么情况下，索引覆盖可能导致性能问题？**  
- 索引列过多导致索引体积大，增加存储和维护成本。  
- 更新频繁时，索引更新开销大。  
- 如果查询列过多，索引覆盖收益不明显，反而可能降低写入性能。

---

**47. 什么是协程，它和线程有什么区别？**  
协程是用户态的轻量级线程，由程序自身调度（协作式调度），切换时保存寄存器状态，开销极小（约几百字节）；线程是内核态，由操作系统抢占式调度，切换需陷入内核，开销大（约1-2MB栈空间）。协程适合 IO 密集型高并发场景，线程适合 CPU 密集型并行计算。Python 中协程通过事件循环调度，线程受 GIL 限制。

---

**48. 协程的优势在于哪里？具体在项目中如何使用？**  
**优势**：高并发、低内存、编程简单（同步写法）。  
**使用**：Python 用 `asyncio`、Go 用 `goroutine`、C++ 用 `co_await`，用于网络请求、数据库操作等。

---

**49. HTTPS 的工作原理是什么？**  
1. 客户端发送 ClientHello，包含支持的加密算法列表。  
2. 服务器返回 ServerHello、证书、ServerKeyExchange（DH参数）。  
3. 客户端验证证书，生成预主密钥，用服务器公钥加密发送。  
4. 双方通过密钥派生生成会话密钥（支持前向保密）。  
5. 之后用对称加密通信，定期更换会话密钥。

---

**50. 如何保证 HTTPS 通信中的数据安全？**  
- 公钥加密传输对称密钥。  
- 证书验证防止中间人。  
- 使用强加密套件（如 TLS 1.3）。  
- 定期更新证书。

---

**51. 如何解决 Leetcode 第 2 题两数相加？**  
遍历两个链表，逐位相加，记录进位。时间复杂度 O(max(m,n))，空间 O(1)（不计结果）。已是最优。

---

**52. 如果要求时间复杂度最优，怎么优化算法？**  
原题已是最优，无法再优化。但可优化代码简洁性或常数时间，如使用哑节点简化边界处理。
